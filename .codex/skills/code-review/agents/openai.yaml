interface:
  display_name: "Code Review"
  short_description: "Review code for bugs and risk"
  default_prompt: "Use $code-review to review this diff for correctness, regressions, and missing tests."

policy:
  allow_implicit_invocation: true
